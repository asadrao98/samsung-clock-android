# Room and Compose need no special rules at this configuration.
# Keep entity classes' field names so Room's generated code resolves them.
-keepclassmembers class com.asadrao.clock.data.db.entity.** { *; }
