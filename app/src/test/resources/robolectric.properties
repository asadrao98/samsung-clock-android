# Robolectric defaults to the app's targetSdk (36), and the API 36 android-all jar
# requires Java 21. This Mac has only Homebrew openjdk@17, so JVM tests run against
# API 35 shadows instead. That is a deliberate, documented gap: anything whose
# behaviour genuinely differs on API 36 must be checked on a real device.
sdk=35
